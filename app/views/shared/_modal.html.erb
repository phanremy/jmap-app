<%# TODO: change modal-display  %>
<%= turbo_frame_tag :post_form_modal,
    data: { "controller": "modal-display" } do %>
  <div class="relative z-30" aria-labelledby="modal-title" role="dialog" aria-modal="true">
    <div class="fixed inset-0 bg-gray-500 bg-opacity-75 transition-opacity duration-300 opacity-0" data-modal-display-target="background"></div>

    <div class="fixed z-10 inset-0 overflow-y-auto">
      <div class="flex items-end sm:items-center justify-center min-h-full p-4 text-center sm:p-0">
        <div class="relative bg-white dark:bg-black text-black dark:text-white rounded-lg px-4 pt-14 pb-4 text-left shadow-xl transform transition-all w-full m-1 ease-out duration-300 opacity-0 translate-y-4 sm:translate-y-0 sm:scale-95"
             data-modal-display-target="panel">
          <div class="absolute top-0 right-0 pt-4 pr-4">
            <button type="button" class="bg-white dark:bg-dark rounded-md text-gray-400 hover:text-gray-500 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-secondary"
                    data-action="click->modal-display#close">
              <span class="sr-only">Close</span>
              <%= svg_tag 'icons/cross' %>
            </button>
          </div>
          <%= content %>
        </div>
      </div>
    </div>
  </div>
<% end %>
