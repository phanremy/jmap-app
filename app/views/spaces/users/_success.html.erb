<%= turbo_frame_tag :space_users do %>
  <p><%= t('alert.done') %></p> <br>
  <%= link_to t('actions.reload'), '#', data: { turbo: false }, onClick: "window.location.reload()", class: "text-secondary-foreground font-bold py-2 px-4 rounded bg-secondary hover:bg-secondary-darker cursor-pointer" %>
<% end %>
